import ContactDetailPage from '@/modules/crm/pages/contactManagement/ContactDetailPage';

/** Detail view for an assigned contact — reuses the shared contact detail page. */
export default function AssignedContactDetailPage() {
  return <ContactDetailPage />;
}
