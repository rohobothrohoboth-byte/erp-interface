# Finance Phase 1 clean integration

This branch is intentionally limited to the Finance reporting integration. Accounting calculations belong to the backend FinanceReports API; the UI should render backend-provided balances and totals rather than reconstruct accounting rules.
