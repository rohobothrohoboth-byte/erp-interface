import AttendanceReport from "@/modules/hr/pages/attendancepage/AttendanceReport";

/**
 * Auth seeder path: /hr/reports/attendance
 * Reuses the existing attendance report UI so the menu path resolves.
 */
export default function AttendanceReportsPage() {
  return <AttendanceReport />;
}
