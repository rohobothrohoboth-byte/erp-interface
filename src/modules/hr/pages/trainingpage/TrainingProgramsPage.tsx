import Training from "@/modules/hr/pages/trainingpage/Training";

/** Auth seeder aliases for /hr/training/* — reuse the main Training module UI. */
export default function TrainingProgramsPage() {
  return <Training />;
}
